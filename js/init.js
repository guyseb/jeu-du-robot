var plateau = new Plateau(window.innerWidth - 30, window.innerHeight - 30);// taille du plateau de jeu
//ecouteur de l'évenemment
document.addEventListener("keydown", function(e){
    hero.deplacement(e);
console.log();
        var plateau = new Plateau(window.innerWidth, window.innerHeight);
         plateau.dessinePlateau();
});